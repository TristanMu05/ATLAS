use atlas_protocol::{decode_packet, Packet};
use std::io::{BufReader, Read};
use std::fs::File;
use std::time::{Instant, Duration};


fn main() -> Result<(), Box<dyn std::error::Error>> {
    
    Ok(())
}
